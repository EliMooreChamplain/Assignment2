vec4 effect1(in vec4 fragColor, in vec2 resolution)
{
    return vec4(0,0,0,0);
}

vec4 effect0(in vec4 fragColor, in vec2 resolution)
{
    vec4 color = vec4(1,1,0,1);
    return color;
}

void mainImage( out vec4 fragColor, in vec2 fragCoord )
{
    fragColor = effect1(fragColor, iResolution.xy, fragCoord, 50);
}

/*
vec4 effect2(in vec4 fragColor, in vec2 resolution, in vec2 fragCoord, in int diameter)
{
    
    //Move the position to the origin
    vec2 relativePos = fragCoord - (resolution * 0.5);
    //Distance from origin is smaller than radius
    if(sqrt(relativePos.x * relativePos.x) + (relativePos.y * relativePos.y) < diameter) // I don't know what I'm doing wrong here
    {
        return vec4(1,0,0,1);
    }
    return vec4(0,0,1,1);
}
*/

